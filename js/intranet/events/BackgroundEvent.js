"use strict";
define(
    function ()
    {
        var module = {
            color:null,
            scheme:"lightly"
        };

        module.CHANGE = 'onBackgroundChange'

        return module;
    });
